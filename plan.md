<proposed_plan> # 基于 BeeCount 定制 Android 截图记账

## 项目与开发方式

- 将 BeeCount fork 到你的 GitHub 账号
- 从当时最新稳定版建立开发分支，记录对应提交；开发前核对它与主分支的相关差异。
- 所有修改提交到自己的 fork，不向原仓库提交 PR。
- 保留 Flutter、SQLite 和现有模块结构，沿用账单、账户、图表及自托管同步能力。
- 遵守当前许可证，保留版权、许可和作者信息；修改后的源码保存在公开 fork 中。

## 专用截图入口与确认流程

核心流程：**下拉“截图记账” → 系统授权截屏 → AI 识别 → 编辑草稿 → 确认入账**。

- 新增 Android 快捷设置按钮，首次使用提供添加引导。
- 移除全局截图自动监听及相关开关；升级时清除旧监听状态。普通截屏不触发识别或上传。
- 使用系统截屏接口，按系统要求获取授权；收起面板和授权界面后捕获目标页面，取到图片立即停止捕获。
- 保留主动分享图片、App 内选图和手动记账入口，不申请持续读取整个相册的权限。
- 无法截屏、授权取消或识别失败时不创建正式账单，可重试、换图或手动填写。

将现有“识别后直接入账”拆成“识别草稿”和“确认保存”两个阶段：

- 草稿包含交易类型、金额、日期、分类、商家、商品描述、支付渠道及资金账户；所有字段均可修改。
- 渠道与账户分别记录，例如“微信支付＋招商银行卡”。
- 一张图包含多笔交易时展示草稿列表，可逐笔纠正、勾选后统一确认。
- 缺失、模糊字段提示补充；不确定的信息不编造，必要字段完整后才能保存。
- 未确认草稿不计入余额、统计和同步。确认操作防止重复提交，疑似重复账单提示用户处理。
- 草稿在本地暂存，支持稍后继续和丢弃；识别重试不能覆盖用户已经修改的内容。

## 图片、AI 与后台运行

- 使用 App 内独立配置的 OpenAI 兼容视觉接口，手机直接调用；沿用并核实现有配置与密钥保护能力。
- 本次定制的图片记账流程不保存截图附件，也不上传图片到同步服务器。
- 识别完成后清理 App 创建的临时图片；失败时仅在当前重试会话内保留，退出后清理。进程异常退出留下的临时文件在下次启动时清理。
- 用户相册、分享来源中的原图不由 App 删除。
- 图片、模型原始响应及接口密钥不写入日志。
- 不为截图记账配置常驻监听、持续屏幕捕获或电池优化豁免。
- 保存后触发同步；失败保留本地待同步记录，采用系统调度的有限重试。打开 App、回到前台及手动刷新时检查远端更新。
- 自建云实时连接仅在前台需要时运行，进入后台关闭连接及心跳；正在执行的保存、识别和同步任务完成后释放资源。

## 账本与自托管同步

首版为人民币、单人、单账本，复用已有账户和统计界面，并核实、补齐以下规则：

- 支持现金、零钱、储蓄卡和信用卡，以及初始余额、收支、内部转账、退款、还款和余额校准。
- 转账和信用卡还款不重复计入收支；信用卡消费计入支出并增加欠款。
- 退款关联原支出，支持部分和多次退款，累计不得超过原交易金额；按退款日期冲减原分类支出。
- 余额校准单独留记录，不计入收入或支出。
- 修改、删除旧记录后正确更新相关账户和统计；多账户交易原子保存。
- 保留消费趋势、分类占比、账户余额及账单查询能力，默认按北京时间统计。

同步采用现有 **BeeCount Cloud**，先保持服务协议兼容，不另写一套服务。新增字段必须支持完整同步与恢复；如确需修改服务端，再 fork BeeCount Cloud，并在自己的仓库维护。

同步只传输已确认记录；验证重试不重复入账、删除可同步、冲突不静默丢失修改。数据库采用明确的版本迁移，保留原有账单及历史附件，新增记账默认不留图片。

## 验证与交付

- 验证识别与入账分离、单笔及多笔草稿、字段校验、纠正内容保护、重复提交和图片清理。
- 验证转账、信用卡还款、部分退款、余额校准，以及历史记录修改、删除后的账务结果。
- 验证旧版本数据迁移、旧截图监听关闭、离线同步重试、冲突处理和新设备恢复。
- 完成相关逻辑测试与 Android 构建，交付 fork 地址、源码提交、本地 APK 和自托管配置说明。
- 前端测试与真机验收由你负责；交付时说明尚未验证的截图授权、系统兼容性和实际耗电表现。
- 本轮开发不自动安装 APK 或部署线上服务。</proposed_plan>